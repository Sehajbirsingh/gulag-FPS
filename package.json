{
  "name": "gulag-fps",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node server/index.js",
    "build": "vite build",
    "start": "NODE_ENV=production node server/index.js",
    "lint": "node --check server/index.js && node --check server/gameServer.js && node --check server/matchManager.js && node --check shared/config.js && node --check shared/hitboxes.js && node --check src/main.js && node --check src/game/Game.js && node --check src/player/PlayerController.js && node --check src/audio/FootstepAudio.js",
    "test": "node --test",
    "qa": "npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "socket.io-client": "^4.7.5",
    "three": "^0.165.0",
    "vite": "^8.1.5"
  }
}
